# 検証記録

実施日：2026年9月19日（日本時間）。実装済みと本番稼働済みを区別します。

## 実施済み

- 気象庁公式の専門家向けページからリンクを発見し、PDFを実際に取得。
- 取得資料：短期予報解説資料1、2026年9月19日03:40発表、朝版（05時予報向け）、1ページ。
- PDFの発表日時・取得日時・元URL・SHA-256を保存。テキスト抽出とページ画像化に成功。
- requirements.txtの固定バージョンを隔離ディレクトリへインストールし、Windows / Python 3.12で30件のユニットテストに成功。
- 重複・途中終了時の課金予約、月間回数制限、再試行上限、引用不一致、不完全API応答、秘密情報混入、HTMLエスケープを検証。
- Responses APIのリクエスト形式（PDFとテキスト、high detail、strict JSON schema、store:false）はモックで検証。タイムアウト時に自動再課金リトライしないことを確認。
- 固定依存バージョンで静的サイト生成と公開成果物検査に成功。
- Edge / Playwrightで320px・390px・1440pxの画面幅を確認。横にはみ出す表示なし。
- 資料選択、航空項目の開閉、原資料画像の拡大・縮小・リセット・Escape、古い資料の警告を確認。JavaScriptコンソールエラーなし。
- モバイル画面と因果関係セクションをスクリーンショットで目視確認。

ブラウザ検証の機械可読結果は `QA-browser.json` にあります。

## 未実施・必要な設定

- 実際のOpenAI API呼び出し：キー未設定のため未実施。モデルの利用権限、実トークン数、実際の説明品質・費用は未確認。
- GitHubリポジトリ作成・push、Secrets登録、Actions実行、Pages公開：保存先未指定のため未実施。
- Linux上のCI、本番の日次スケジュール、公開URLでのスマートフォン閲覧：未実施。
- 画面デモは明示した架空データ。取得した9月19日の資料をAIが解析した結果ではありません。
- 関連する高層・FAX天気図は公式リンク案内のみ。自動取得・解析には含めていません。

## 初回クラウド実行時の確認

READMEの手順でSecretsとPagesを設定して手動実行し、Actionsの生成・公開が成功することを確認してください。公開画面の発表日時、生成日時、モデル、原資料、根拠引用を照合し、`data/reports/` のusageで実消費量を確認します。同じPDFで再実行して `unchanged` となり、新規API解析が行われないことも確認してください。

GitHub Actionsの定時実行には遅延・停止の可能性があり、予定時刻どおりの実行保証ではありません。画面の取得・発表日時と更新警告を確認してください。
