# GitHub: Settings > Secrets and variables > Actions
# Set OPENAI_API_KEY as a secret. Never commit an actual key.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
MAX_MONTHLY_REQUESTS=70
MAX_OUTPUT_TOKENS=12000
# Phase 1 reads the short-range PDF only. Related charts are reference links.
